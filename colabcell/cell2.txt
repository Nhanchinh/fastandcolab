# =============================
# Cell 2: Install libraries (FINAL)
# =============================
# Core ML (pin versions để tránh lỗi tương thích)
!pip install -q transformers==4.46.3 tokenizers sentencepiece
!pip install -q torch accelerate bitsandbytes
!pip install -q huggingface_hub numpy

# API Server
!pip install -q fastapi uvicorn pyngrok nest-asyncio

# Vietnamese NLP
!pip install -q underthesea

# Evaluation metrics (chạy trên GPU cho nhanh)
!pip install -q evaluate bert_score rouge_score pyvi

print("✅ Đã cài đặt thư viện!")